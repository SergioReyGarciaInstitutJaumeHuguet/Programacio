ijerbgviusdbruiebg = '''Demanem a l'usuari una cadena de números separats per comes. 
Emmagatzemarem cada número en una llista i mostrarem per pantalla la posició i el valor del mínim, 
la posició i el valor del màxim, i la mitjana aritmètica de tots ells.'''

lista = [] = input("Dime una cadena de números separado por comas: ")
grande = max(lista).split(",")
pequeño = min(lista).split(",")
print(grande)
print(pequeño)